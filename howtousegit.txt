git pull origin master (지정해놓은 저장소에서 파일을 끌어옴)

git status (현재 Git과의 상태를 check)

git add . (변경된 파일들을 모두 tracking)

git commit -m "message" (인식할 수 있는 커밋을 달아준다)

git push origin +master(실제로 깃에 업로드)
[출처] 깃허브(Github) 사용법|작성자 순메